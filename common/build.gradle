plugins {
    id 'java'
    id 'org.spongepowered.gradle.vanilla' version "${vanilla_gradle_version}"
}

version = "${project.mod_version}"
group = project.mod_group

minecraft {
    version("${minecraft_version}")

    def accessWidenerFile = file("src/main/resources/${mod_id}.accesswidener")
    if (accessWidenerFile.exists()) {
        accessWideners(accessWidenerFile)
    }

    runs {
    }
}

dependencies {
    compileOnly group: 'org.spongepowered', name: 'mixin', version: "${mixin_version}"
    compileOnly("dev.toma.configuration:configuration-common:${configuration_version}")

    // TODO enable and fix version once JEI is released
    compileOnly("mezz.jei:jei-26.1.2-common-api:${jei_version}")
    //runtimeOnly("mezz.jei:jei-${minecraft_version}-common:${jei_version}")
}